import React, { Component } from 'react';

export default class GameMatcher extends Component {
    render() {
        console.log(this.props);
        return (
            <div>Game Matcher</div>
        );
    }
}